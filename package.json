{
  "private": true,
  "name": "worker",
  "version": "1.0.0",
  "description": "Given a request, redirects the user to one of two website variants",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'",
    "build": "webpack index.js"
  },
  "author": "Michael Zhu",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
